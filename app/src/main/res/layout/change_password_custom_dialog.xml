<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="0sp"
            android:layout_height="wrap_content"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:orientation="vertical"
            android:paddingTop="30sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintWidth_percent=".8">

            <EditText
                android:id="@+id/oldPasswordField"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="15sp"
                android:background="@drawable/ic_outlined_button_light"
                android:drawableStart="@drawable/ic_password_light"
                android:drawablePadding="10sp"
                android:hint="@string/old_password"
                android:importantForAutofill="no"
                android:inputType="textPassword"
                android:padding="10sp"
                android:textColor="@color/quaternaryLight"
                android:textColorHint="@color/quaternaryLight"
                android:textCursorDrawable="@color/quaternaryLight"
                android:textSize="18sp" />

            <EditText
                android:id="@+id/newPasswordField"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="15sp"
                android:background="@drawable/ic_outlined_button_light"
                android:drawableStart="@drawable/ic_password_light"
                android:drawablePadding="10sp"
                android:hint="@string/new_password"
                android:importantForAutofill="no"
                android:inputType="textPassword"
                android:padding="10sp"
                android:textColor="@color/quaternaryLight"
                android:textColorHint="@color/quaternaryLight"
                android:textCursorDrawable="@color/quaternaryLight"
                android:textSize="18sp" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>